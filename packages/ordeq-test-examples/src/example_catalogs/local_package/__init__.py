# This package catalog contains exactly the same IO names as
# example_catalogs/local.py, but is structured as a package instead of a
# single module.
from ordeq_common import StringBuffer

hello = StringBuffer("Hello from local package")
result = StringBuffer()
