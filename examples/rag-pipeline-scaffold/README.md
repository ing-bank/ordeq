# Ordeq RAG pipeline scaffold

This example demonstrates how to create a Retrieval-Augmented Generation (RAG) pipeline scaffold using Ordeq.

To run this example:

```shell
uv run python -m rag_pipeline
```
